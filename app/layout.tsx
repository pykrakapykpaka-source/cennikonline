import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Cennik online – tworzymy profesjonalne cenniki dla firm",
    template: "%s — CennikOnline.pl",
  },
  description:
    "Zamów cennik online dopasowany do Twojej branży. Szybka realizacja, wersja mobilna, podstawowe SEO i gotowy link do udostępnienia klientom.",
  applicationName: "CennikOnline.pl",
  robots: { index: true, follow: true },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pl">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
